# Migraciones

Las migraciones incrementales ya se aplicaron. El esquema actual está en:

**`../ddl-completo-elevenpray.sql`**

Usa ese archivo como referencia única del modelo de datos y para crear una base nueva desde cero.
