# Copia a .env.local y ajusta si hace falta
# Puerto donde corre el frontend (Next.js)
PORT=3000
# URL del backend (local). En Vercel con Services, usa NEXT_PUBLIC_BACKEND_URL (/_/backend) o déjalo sin definir.
NEXT_PUBLIC_API_URL=http://localhost:8080
